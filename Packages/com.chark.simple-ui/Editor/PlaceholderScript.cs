namespace CHARK.SimpleUI.Editor
{
    // Added to prevent editor warnings, one we get some scripts in Editor namespace, we should
    // yeet this.
    internal sealed class PlaceholderScript
    {
    }
}
